rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tactics collection: allow read and write so coaches can share schemes via ?id=
    match /tactics/{tacticId} {
      allow read, write: if true;
    }
  }
}
